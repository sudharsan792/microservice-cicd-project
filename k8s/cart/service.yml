apiVersion: v1
kind: Service
metadata:
  name: cart-service
  namespace: webapps
spec:
  selector:
    app: cart-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5001
  type: LoadBalancer
