apiVersion: apps/v1
kind: Deployment
metadata:
  name: cart-service
  namespace: webapps
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cart-service
  template:
    metadata:
      labels:
        app: cart-service
    spec:
      containers:
        - name: cart-service
          image: sudharsan4362/cart-service:latest
          ports:
            - containerPort: 5001
