apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: webapps
spec:
  selector:
    app: product-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: LoadBalancer